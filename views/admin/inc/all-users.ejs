<table>
    <thead>
        <tr>
            <th>Full Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>

    <tbody id="userTable">

        <!-- Example row -->
        <% users.forEach(user=> { %>
            <tr>
                <td>
                    <%= user.name %>
                </td>
                <td>
                    <%= user.email %>
                </td>
                <td>
                    <a href="/admin/update/<%= user.id %>" class="btn update">Update</a>
                    <a href="" class="btn delete">Delete</a>
                </td>
            </tr>
            <% }) %>
    </tbody>
</table>